function sortOnPrice(myList) {
    myList.sort((a, b) => a.price - b.price);
    console.log(myList)
}

function sortOn(myList) {
    myList.sort((a, b) => a.price - b.price);
    console.log(myList)
}

function sortOnPrice(myList) {
    myList.sort((a, b) => a.price - b.price);
    console.log(myList)
}